# 🧩 C++ Matrix Loop Logic Test

![Language](https://img.shields.io/badge/Language-C%2B%2B-blue.svg)
![Type](https://img.shields.io/badge/Type-Logic%20Test-orange.svg)
![Environment](https://img.shields.io/badge/Environment-Console-lightgrey.svg)

## 📌 Giới thiệu (Introduction)
Code nhỏ này chứa một chương trình C++ mẫu với các cấu trúc vòng lặp lồng nhau (`nested loops`) và câu lệnh điều kiện (`if/else`) phức tạp. Mục đích chính của dự án là để **kiểm tra tư duy logic thuật toán**, tính toán tọa độ và điều khiển luồng dữ liệu để vẽ các họa tiết ma trận (matrix pattern) bằng ký tự trực tiếp trên màn hình Console (Terminal).

*Lưu ý: Mã nguồn chi tiết nằm trong tệp `.cpp` đính kèm tại kho lưu trữ này.*

## 👁️ Xem trước (Review)

Kết quả in ra màn hình khi biên dịch và chạy chương trình thành công:

![Pattern Preview](https://github.com/user-attachments/assets/bcd2ccdd-7ca5-4d23-babb-0bb625debc09)

---

## 🚀 Hướng dẫn biên dịch và chạy (Compilation & Execution)

Nếu bạn đang sử dụng Terminal (Linux/macOS) hoặc Command Prompt (Windows) có cài sẵn trình biên dịch G++, hãy thực hiện các bước sau để chạy file mã nguồn (giả sử tên file của bạn là `main.cpp`):

1. **Biên dịch mã nguồn:**
   ```bash
   g++ -o matrix_loop main.cpp
   ```

2. **Chạy tập tin thực thi:**
   * Trên Linux/macOS:
     ```bash
     ./matrix_loop
     ```
   * Trên Windows:
     ```cmd
     matrix_loop.exe
     ```
